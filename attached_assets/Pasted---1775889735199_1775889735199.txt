从当前线上页面和截图看，你这次并没有完成我的核心需求。你之前的汇报把“地址切换完成”和“核心功能完成”混为一谈了。现在请你停止泛泛汇报，按真实状态修正，并且**全程只用中文回复我**。

先明确现状：

1. 右上角连接的仍然是 Solana 钱包，不是 BSC / EVM 钱包
2. 我链接 BSC 钱包后，首页并没有正确刷新出该 EVM 地址对应的代币余额
3. Token Overview 的价格、市值、流动性、持有人、24h 成交量没有实时更新
4. 四大金库、分红、LP、推荐佣金、营销金库目前仍然只是静态占位，并没有真实链上合约支撑
5. 当前页面不能宣称这些核心机制已经完成

现在按下面顺序处理，并且不要跑题去修无关漏洞，除非我明确要求。

---

## 第一部分：先修正钱包连接逻辑（最高优先级）

目标：让首页 Dashboard 真正以 **EVM / BSC 钱包** 为主，不再被 Solana 钱包状态干扰。

要求：

1. 首页和 Token Dashboard 的主钱包状态，必须来自 **EVM 钱包**

* 使用 MetaMask / injected provider / wagmi / viem
* 当前地址、当前链、余额读取都基于 EVM 钱包
* 不要再让首页主面板依赖 Solana wallet adapter

2. 明确区分两套钱包

* Solana 钱包：只服务原有 Solana 相关页面
* EVM 钱包：服务首页 Token Dashboard、BSC 代币余额、后续 claim 逻辑

3. 首页右上角如果仍保留 Solana 钱包入口

* 必须在首页增加明确的 **EVM Connect** 入口
* 或者首页 Token Dashboard 内部提供一个清晰的 EVM 钱包连接按钮
* 总之，用户必须能在首页直接连接 BSC 钱包，而不是只看到 Solana 钱包

4. 修好后我要看到：

* EVM 地址
* 当前链名称
* 是否在 BSC Mainnet
* 对应地址的 token balance
* 达标 / 不达标状态（200,000 门槛）

---

## 第二部分：修正实时数据逻辑

现在只把地址切换了，但实时数据链路没完成。

要求你查清楚并修正：

1. 这个测试代币 `0x0a9c2e3cda80a828334bfa2577a75a85229f7777` 是否已经有真实交易对

* 是否在 Pancake / Flap / 其他 DEX 上有 pair
* 是否已被 DexScreener / GeckoTerminal 索引

2. 如果已经有 pair 且已被索引

* 价格、市值、流动性、24h 成交量、holders 必须接真实数据
* 给我明确数据源和 adapter 逻辑

3. 如果没有被索引

* 不要再假装“实时行情已完成”
* 直接告诉我当前为什么是 `--`
* 并补一个 **链上 pair 读取方案**

  * 如果已有 pair 地址，就直接读 reserves
  * 自己计算 price / liquidity
* 如果连 pair 都没有，就明确告诉我：没有交易对，所以当前无法产生实时价格和流动性

4. holders 如果第三方 API 不稳定

* 给我一个可执行的替代方案
* 比如保留第三方 API + fallback 说明
* 但不要继续模糊表述成“已真实查询”就算完成

---

## 第三部分：把“静态占位”和“真实功能”严格区分

你之前的汇报把未来功能说得像已经完成了，这不可以。

现在请你把 Dashboard 分成两类：

### A. 已真实实现的

只允许包括：

* 链上 token metadata
* EVM 钱包地址 / 当前链 / token balance
* explorer links
* 如果有真实 pair / 行情数据，再算上市场数据

### B. 仍然只是预留的

必须明确标出来：

* claimableHolderReward
* earnedLP
* pendingReferral
* pendingBnbRewards
* pendingLpRewards
* holdingWeight
* timeMultiplier
* 各类 vault balances（如果地址未部署）
* 推荐佣金自动审核 / 自动发放
* LP 自动分红
* holder 自动分红 claim

页面里对于 B 类功能，不要伪装成已经可用。

---

## 第四部分：推进真正的核心功能开发

我要的核心不是“页面有四个卡片”，而是这些机制真正能落链。

目标机制：

* 买 3% / 卖 6%
* 税金 100% 自动进入四大金库
* 30% 持币分红金库
* 30% LP 分红金库
* 30% 工作室推广佣金金库
* 10% 营销金库
* 资金不能先进入普通钱包再人工分配
* 用户自己 claim，不做管理员随意提走

你现在请开始做真正的实现准备：

1. 先检查当前仓库里已有的：

* `contracts/MasterVault.sol`
* `contracts/LPRewardVault.sol`
* `contracts/ReferralVault.sol`
* `contracts/MarketingVault.sol`
* `docs/architecture/reward-vault-architecture.md`

2. 不要只说“文件存在”
   要逐个核验：

* 接口是否完整
* 事件是否齐全
* basis points 是否正确
* 是否有重入保护
* 是否真的是“税进合约再分配”
* 是否和当前前端字段能对应上

3. 然后给我一份中文报告：

* 现在真正已完成的是什么
* 未完成的是什么
* 先做哪一部分最合理
* 哪些功能需要我后续提供测试部署钱包（不是现在）

---

## 第五部分：这次必须交付的内容

按下面格式回我，并且全程中文：

1. 当前首页 EVM 钱包连接链路是否已修好
2. 当前首页是否能正确读取 BSC 钱包地址下的 token balance
3. 当前价格 / 市值 / 流动性 / 24h / holders 为什么没有实时更新
4. 这个测试币当前是否已有真实交易对和索引
5. 当前哪些功能是真实可用的
6. 当前哪些功能仍然只是占位
7. 合约草案逐个核验结果
8. 下一步最小可执行开发计划
9. GitHub HEAD 提交号

重点：

* 不要再把“地址切换完成”说成“核心功能完成”
* 不要再跑题去修无关漏洞
* 不要再用英文回复我
