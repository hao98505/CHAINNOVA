不要继续开发，不要再说“已完成”。先按 GitHub main 缺失项做一次精准补丁，并且只用中文极简回复。

本轮目标：
把仓库真正改成 Solana ↔ BSC / Arbitrum / Ethereum 的 Bridge v3 最小落仓版本。

只允许修改这些文件：
1. client/src/pages/Bridge.tsx
2. client/src/lib/evmBridge.ts
3. client/src/lib/solanaBridge.ts（新建）
4. client/src/lib/bridgeRouter.ts（新建）
5. server/solana-watcher.ts（新建）
6. server/bridge-relayer.ts
7. .env.example
8. docs/bridge-v2.md
9. package.json

硬性要求：
A. Bridge.tsx
- 标题必须改成：Bridge v3 · Solana ↔ EVM
- 源链必须支持：Solana / BSC / Arbitrum / Ethereum
- 目标链必须支持另外三条
- 页面里不得再出现这些旧文案：
  - Bridge v2 · EVM
  - BSC ERC20 跨链桥
  - 源链固定为 BSC
  - 目标链支持 opBNB 和 Arbitrum
- 不得再出现 opBNB 选项
- 必须支持：
  - Solana 钱包（Phantom / Solflare）
  - EVM injected wallet
  - Solana -> BSC/Arbitrum/Ethereum
  - BSC/Arbitrum/Ethereum -> Solana

B. 必须新建并写入仓库：
- client/src/lib/solanaBridge.ts
- client/src/lib/bridgeRouter.ts
- server/solana-watcher.ts

C. .env.example
必须改成 Solana ↔ EVM v3 变量，不得保留 opBNB 专用变量。
至少要有：
- VITE_SOLANA_RPC_URL
- VITE_SOLANA_MINT=6ZcR1KCqVZDLzSoUbiPW8P6XUvrazxMtUZTa9csppump
- VITE_SOLANA_VAULT
- VITE_BRIDGE_BSC
- VITE_BRIDGE_ARBITRUM
- VITE_BRIDGE_ETHEREUM
- VITE_WRAPPED_FORGAI_BSC
- VITE_WRAPPED_FORGAI_ARBITRUM
- VITE_WRAPPED_FORGAI_ETHEREUM
- SOLANA_RPC_URL
- SOLANA_VAULT_KEYPAIR_PATH
- SOLANA_VAULT_OWNER
- SOLANA_VAULT_ATA
- SOLANA_MINT=6ZcR1KCqVZDLzSoUbiPW8P6XUvrazxMtUZTa9csppump
- BSC_RPC_URL
- ARBITRUM_RPC_URL
- ETHEREUM_RPC_URL
- RELAYER_PRIVATE_KEY
- VALIDATOR_PRIVATE_KEY
- BSC_BRIDGE
- ARBITRUM_BRIDGE
- ETHEREUM_BRIDGE
- WRAPPED_FORGAI_BSC
- WRAPPED_FORGAI_ARBITRUM
- WRAPPED_FORGAI_ETHEREUM
- STATE_FILE_PATH

D. docs/bridge-v2.md
- 内容改成 Bridge v3 · Solana ↔ EVM
- 删除所有 opBNB 内容
- 删除 BSC-only 说明
- 写清楚 custodial MVP
- 写清楚 Solana -> EVM 和 EVM -> Solana
- 写清楚三条目标链：BSC / Arbitrum / Ethereum

E. package.json
必须真正写入这些 scripts：
- bridge:watch:solana
- bridge:watch:evm
- bridge:dev
- bridge:deploy:eth
- bridge:config:eth
并删除或停用 opbnb 相关 bridge scripts

完成后只输出 5 行：
1. 已修改文件：
2. 新增文件：
3. 删除的旧文案 / 旧脚本：
4. 本地验证结果：
5. 还剩 blocker：