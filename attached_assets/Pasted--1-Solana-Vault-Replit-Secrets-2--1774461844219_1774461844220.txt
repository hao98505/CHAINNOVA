不要改桥接业务逻辑，先只做两件事：
1. 自动生成 Solana Vault 相关值并尽量写入 Replit Secrets
2. 顺手检查部署脚本入口是否和当前仓库文件一致

严格要求：
- 不要改 Bridge 页面
- 不要改 watcher/relayer 业务逻辑
- 不要改合约逻辑
- 只在“脚本入口不一致”时，最小范围修正 package.json 的 bridge:deploy:* / bridge:config:* 指向
- 全程中文回复
- 不要输出私钥内容
- 如果某一步做不到，明确说明原因

按顺序执行：

第一步：检查部署脚本入口
- 检查 package.json 里的以下脚本实际指向的文件是否存在且可执行：
  - bridge:deploy:bsc
  - bridge:deploy:arb
  - bridge:deploy:eth
  - bridge:config:bsc
  - bridge:config:arb
  - bridge:config:eth
- 如果 package.json 仍指向旧的 .ts 文件，但当前实际可运行的是 scripts/deploy.cjs / scripts/configRoutes.cjs，就只修正这些脚本入口
- 除此之外不要改任何代码文件

第二步：检查 Solana CLI
- 检查是否存在：
  - solana-keygen
  - spl-token
- 如果缺失，就安装 Solana CLI / SPL Token CLI
- 安装完成后继续，不要停

第三步：确定 Solana RPC
- 如果 Secrets 中已有 SOLANA_RPC_URL，就使用它
- 如果没有，就临时使用：
  https://api.mainnet-beta.solana.com
- 同时把 VITE_SOLANA_RPC_URL 设置成同一个值
- 然后先验证这个 mint 在当前 RPC 对应网络上真实存在：
  6ZcR1KCqVZDLzSoUbiPW8P6XUvrazxMtUZTa9csppump
- 如果 mint 不存在，就停止并明确告诉我是“RPC 网络不对”

第四步：自动生成 Solana Vault
- 如果 /home/runner/workspace/solana-vault.json 不存在，就生成：
  /home/runner/workspace/solana-vault.json
- 读取 Vault owner 公钥
- 为这个 owner 创建该 mint 的 token account / ATA
- 拿到最终的 Vault ATA 地址
- 不要输出私钥内容，只输出文件路径和公钥/ATA

第五步：尽量自动写入 Replit Secrets
如果 Replit 环境允许写入 Secrets / env，就写入以下键：
- SOLANA_RPC_URL
- VITE_SOLANA_RPC_URL
- SOLANA_VAULT_KEYPAIR_PATH=/home/runner/workspace/solana-vault.json
- SOLANA_VAULT_OWNER=<自动生成的 owner 公钥>
- SOLANA_VAULT_ATA=<自动生成的 ATA>
- VITE_SOLANA_VAULT=<自动生成的 ATA>
- SOLANA_MINT=6ZcR1KCqVZDLzSoUbiPW8P6XUvrazxMtUZTa9csppump
- VITE_SOLANA_MINT=6ZcR1KCqVZDLzSoUbiPW8P6XUvrazxMtUZTa9csppump

如果不能自动写入 Secrets，就把这 8 个键和值整理成一张表让我手动填。

第六步：检查 RELAYER_PRIVATE_KEY
- 如果缺少 RELAYER_PRIVATE_KEY，就明确告诉我“请手动把 RELAYER_PRIVATE_KEY 填成和 PRIVATE_KEY 一样（仅限测试）”
- 不要输出 PRIVATE_KEY 原文

第七步：如果以上都准备好了，尝试启动：
- npm run bridge:watch:solana
- npm run bridge:watch:evm
如果启动失败：
- 明确区分是 Secrets 问题、RPC 问题、Vault 问题，还是脚本问题

最后只按这个格式回复：
1. 部署脚本入口检查结果：
2. Solana CLI 检查结果：
3. Solana mint 网络检查结果：
4. Vault 生成结果：
5. 需要写入的 Secrets：
6. watcher 启动结果：
7. 还剩 blocker：
8. 我下一步只需要做什么：